export const deadCell = (emoteDead = '🌑') => {
  const deadCell = emoteDead;
  return deadCell;
};

export const aliveCell = (emoteAlive = '🧫') => {
  const aliveCell = emoteAlive;
  return aliveCell;
};
